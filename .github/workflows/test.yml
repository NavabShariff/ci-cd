name: test

on: 
 push:
  branches: ['main']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
     
    - name: test
      uses: gitleaks/gitleaks-action@v2
      with:
       fail: true
       redact: true
       verbose: true